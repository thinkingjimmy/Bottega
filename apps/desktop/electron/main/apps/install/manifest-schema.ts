/**
 * [INPUT]: Depends on the canonical cloud-protocol App manifest module.
 * [OUTPUT]: Re-exports the canonical App manifest parser and JSON Schema.
 * [POS]: Desktop import boundary; schema ownership lives in the shared package.
 */
export * from "@ai-chat/cloud-protocol/apps/schemas/manifest";
