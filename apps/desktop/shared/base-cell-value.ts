/**
 * [INPUT]: Depends on @ai-chat/base-ui/compute/base-cell-value.
 * [OUTPUT]: Re-exports the canonical Base types, validation and computation for desktop consumers.
 * [POS]: Desktop import boundary; the shared package owns the implementation.
 */
export * from "@ai-chat/base-ui/compute/base-cell-value";
