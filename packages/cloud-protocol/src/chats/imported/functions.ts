/**
 * [INPUT]: The sole encrypted imported-history registry.
 * [OUTPUT]: Required-space ciphertext generation publication and active membership reads.
 * [POS]: Stable domain registry path; plaintext import DTOs are client-only.
 */
export { importedFunctions } from "./encrypted/functions";
