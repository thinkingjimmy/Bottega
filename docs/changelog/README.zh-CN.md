# 更新日志

[文档首页](../README.zh-CN.md) · [English](./README.md)

本文件只记录产品里程碑，不记录内部实现的逐次迭代。日期表示对应能力首次形成完整产品形态的时间。

## 2026-09-24 — v0.1.8

**升级前：** 无需任何准备。0.1.8 原样打开 0.1.7 的 Chat 数据库、设置与 Bottega 文件夹，云端数据也保留。同步协议升到 10，同一账号下的每台电脑都应更新：0.1.7 的桌面会被要求先更新才能继续同步。见[升级说明](../getting-started/README.zh-CN.md#升级到-018)。

### 新增与改进

- **Bottega Dock（macOS 15 及以上，Apple 芯片）。** 一条程序坞，放你在 Bottega 里做的 App、其它应用、访达、下载、废纸篓，以及每个 Agent 一个 AI 额度环。它可以停在系统程序坞上方与之并存，也可以替代它：此时 Bottega 把系统程序坞设为自动隐藏，并在后台项目里加一个小的恢复项，Bottega 意外停止时由它恢复系统程序坞。在 设置 › Dock 里开启；**恢复系统程序坞** 始终可用——在设置里、在 Dock 的右键菜单里，也在 Bottega 的菜单栏菜单里。
- **Dock 布局跟随账号。** Dock 布局端到端加密，在你的各台电脑之间同步。两台电脑同时修改时，Bottega 能合并的自动合并，其余的询问你。
- **设置处处同一种写法。** Dock、同步与 Memory 页改用与其它设置页相同的分组与列表行；错误与进度就显示在所属的那一行，分步设置统一在同一种弹窗里完成。引导页换成左侧步骤栏的新布局。
- **移动 Bottega 文件夹。** 设置 › 通用 里的 **移动文件夹** 会在 Bottega 重启时移动文件夹，所有保存的路径随之更新。移到另一块磁盘时会先完整复制并核对，旧副本随后移入废纸篓。
- **清空所有数据。** 设置 › 通用 › 危险操作 里的 **清空这台电脑上的所有数据** 会删除这台电脑上的对话、设置、密钥与登录状态，并重新从引导开始；也可以同时把 Bottega 文件夹移入废纸篓。云端数据保留，再次登录后会回来。
- **Bottega 文件夹被删除后重新进入引导**，不再停在恢复对话框；已无法打开的对话副本会移入文件夹的 `.trash`，只提示一次。
- **协议 10。** 同步协议前进一步，承载加密的 Dock 布局。本地数据不变。

### 下载与安装

下方提供 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建**未签名、未公证**。macOS 仍是主要平台；Windows/Linux 的原生 App 隔离、Bottega Dock 及完整功能对齐仍在推进。

**macOS（Apple 芯片）：** 打开 DMG，把 Bottega 拖入「应用程序」。未签名下载需要在终端一次性移除该应用的隔离标记，然后再打开：

```bash
xattr -rd com.apple.quarantine /Applications/Bottega.app
```

**Windows（x64）：** 运行安装包。如果 SmartScreen 拦截未知发布者，点击**更多信息 → 仍要运行**。

**Linux（x64）：** 添加可执行权限后启动 AppImage：

```bash
chmod +x Bottega-0.1.8-linux-x86_64.AppImage
./Bottega-0.1.8-linux-x86_64.AppImage
```

开始对话前，请先安装并登录至少一个受支持的本机 CLI。0.1.0 或 0.1.1 用户需要手动安装 0.1.8，因为旧版本仍有此前的更新器问题。

## 2026-09-23 — v0.1.7

**升级前：** 无需任何准备。0.1.7 原样打开 0.1.6 的 Chat 数据库与设置，云端数据也保留。同步协议升到 9，同一账号下的每台电脑都应更新：0.1.6 的桌面会被要求先更新才能继续同步。已有的同步密码照常可用。见[升级说明](../getting-started/README.zh-CN.md#升级到-017)。

### 新增与改进

- **设置：Providers、Updates、Community。** Providers 决定新 Chat 默认使用的 Agent，以及所有 Agent 选择器里的排列顺序。Updates 把 Bottega 与每个已安装的 Provider CLI 及其版本列在一起，可逐行更新，也可 **全部更新**——先更新各 Provider，最后更新 Bottega，因为它会重启。Community 保留 GitHub、问题反馈与发布说明链接。原来的 About 与 Backends 页面已移除；安装与登录 Agent 仍在 Agent 设置流程里。
- **Agent 暂时接不住的插话不再失败。** Agent 工作时你发出的消息，若它支持插话就在当前这一轮使用；若不支持（Kimi Code 与 OpenCode），消息会被保留，本轮一结束就发出；从另一台设备发出时，回执会写明「将在本轮结束后发送」。只有 Agent 完全无法接受的输入才会被拒绝，草稿保留。若本轮已经结束，被拒的插话会提供 **作为新消息发送**。
- **远程附件发出前先清理。** 发往另一台电脑时——无论从 Cloud Web 还是从驱动远端 Chat 的桌面——照片最长边缩到 2048 px，相机元数据（位置、设备、EXIF / XMP）、GIF 注释块等内嵌元数据都会被移除；文件按真实格式校验。浏览器能读取的 HEIC 照片会自动转换，读不了时给出明确说明。已上传但 20 小时未发送的附件会提示重新上传，而不是在发送时失败；Cloud Web 也不再把自己刚上传的文件从云端下载回来显示。
- **更强的同步密码。** 新设定的同步密码至少 12 个字符，包含字母与数字，至少 5 个不同字符，不能以大段重复或连续字符为主，不能包含邮箱名、`bottega` 或最常见的密码。表单会随输入逐条打勾。已有的密码照常解锁。
- **离线的电脑会说离线了多久。** 没醒着的电脑会显示「休眠」「离线」或「断网」以及距上次在线的时间，直接显示在输入框里。
- **协议 9。** 同步协议前进一步，承载新的插话结果，并为即将到来的手机 App 准备服务端。本地数据不变。

### 下载与安装

下方提供 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建**未签名、未公证**。macOS 仍是主要平台；Windows/Linux 原生 App 隔离及完整功能对齐仍在推进。

**macOS（Apple 芯片）：** 打开 DMG，把 Bottega 拖入「应用程序」。未签名下载需要在终端一次性移除该应用的隔离标记，然后再打开：

```bash
xattr -rd com.apple.quarantine /Applications/Bottega.app
```

**Windows（x64）：** 运行安装包。如果 SmartScreen 拦截未知发布者，点击**更多信息 → 仍要运行**。

**Linux（x64）：** 添加可执行权限后启动 AppImage：

```bash
chmod +x Bottega-0.1.7-linux-x86_64.AppImage
./Bottega-0.1.7-linux-x86_64.AppImage
```

开始对话前，请先安装并登录至少一个受支持的本机 CLI。0.1.0 或 0.1.1 用户需要手动安装 0.1.7，因为旧版本仍有此前的更新器问题。所有旧版本升级前都需要完成上面的存储准备。

## 2026-09-22 — v0.1.6

**升级前请先备份：** 0.1.6 不会打开 0.1.5 的 Chat 数据库。首次启动时，原来的 `bottega.sqlite3` 及其附属文件会被原字节移到应用数据目录下的 `recovery/sqlite/`，对话索引从你的 Bottega 文件夹重建；未写完的回复可能缺失，搜索索引与同步状态会重新建立。0.1.5 的云端数据同样不保留：同步协议已更换，服务在发布前会被重置，请重新登录，由第一台电脑重新设定同步密码。安装前请完全退出 Bottega，并同时备份 Bottega 文件夹与完整的应用数据目录。操作方式见[升级说明](../getting-started/README.zh-CN.md#升级到-016)。

### 新增与改进

- **一条对话属于创建它的那台电脑。** Bottega 不再把运行中的对话在电脑之间搬来搬去。本地就是本地，远程就是远程：你打开持有这份工作的电脑，然后在任何地方操作它。执行者选择、交接与跨电脑队列一并退役，它们过去向你提出的问题也一起消失。
- **登录即远程操作。** 一台登录的电脑会发布自己的侧栏——它的 Project 与 Chat——并接受针对它们的命令。产品里没有第二个开关：设置的同步一栏只有登录状态、本机显示名和退出登录。首次设置不再询问「只在这台电脑上使用还是连接账号」，所有人都从本地开始，登录是之后的步骤。同步密码仍然在登录流程里：第一台电脑设定，之后的每台电脑、浏览器与手机输入。
- **在侧栏里切换电脑。** Cloud Web、手机浏览器与桌面共用侧栏顶部的一条电脑切换器，一台电脑一个标签，醒着时带小圆点，离线时显示「离线 · 5 分钟前」。账号里出现第二台电脑后才出现，桌面永远把自己排在第一位。电脑可在设置里改名；新电脑注册时撞名会自动变成「…（2）」。账号下还没有任何电脑时，浏览器会被告知先在一台电脑上登录，而不会被要求输入一个它无法设定的密码。
- **使用另一台电脑的 Project。** 远端 Project 带地球角标与归属电脑名，不提供目录、路径或「选择文件夹」——它在这里本来就没有目录。在它下面新建的 Chat 会在那台电脑上创建、执行与存储。桌面侧栏 Projects 分组的 `+` 现在提供**本地 Project** 或 **钉住远端 Project…**：钉住只是把另一台电脑的 Project 放进本机侧栏，不复制任何内容；取消钉住对拥有者毫无影响；拥有者删除或归档后，该行仍在并标注状态，菜单里只剩「取消钉住」。
- **电脑离线不再挡住你的修改。** 改名、归档、调整顺序、编辑 Base 行、写 App 记录在拥有它们的电脑睡着时照常工作，等它醒来后对账。需要它醒着的只有执行类动作——发送、停止、批准、回答、插话、删除 Chat——这些控件就地置灰并说明原因，而不是把草稿一起收走，电脑回来后自行恢复。两个设备同时回答同一个权限请求时，第二个得到的是一行「已在 *电脑* 处理」，而不是一个错误。
- **一个 Bottega 文件夹属于一台电脑。** 第一次同步会记录归属，既记在账号里，也写进文件夹。同一台电脑在重装、清空数据或换 profile 后零操作接管；另一台电脑被指名拒绝，并用一句话说明该怎么做。从未同步过的文件夹在任何电脑上都能打开。一台电脑已发布的内容，无论它是否醒着，都能在浏览器和你的其他电脑上读取。
- **导入的对话在哪里都能继续。** 导入的 Codex、Claude Code 或 Kimi 历史，在全新档案里、从文件夹重建的工作区里、或 Project 尚未选择目录时，都不再变成只读。它用普通输入框打开，转录里只有一条分隔线标出导入历史的结束点。
- **首次上传说真话，也更短。** 设置 › Sync 现在按真实上传字节计数，不再停在 `0 B`，并且正好停在自己的总量上；小的对话先传，几秒内数字就开始走。每条消息的网络往返约减少一半。
- **协议 8。** 同步协议与本机 Chat 数据库同时前进，没有兼容层，这也是上面那段升级说明的由来。

### 下载与安装

下方提供 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建**未签名、未公证**。macOS 仍是主要平台；Windows/Linux 原生 App 隔离及完整功能对齐仍在推进。

**macOS（Apple 芯片）：** 打开 DMG，把 Bottega 拖入「应用程序」。未签名下载需要在终端一次性移除该应用的隔离标记，然后再打开：

```bash
xattr -rd com.apple.quarantine /Applications/Bottega.app
```

**Windows（x64）：** 运行安装包。如果 SmartScreen 拦截未知发布者，点击**更多信息 → 仍要运行**。

**Linux（x64）：** 添加可执行权限后启动 AppImage：

```bash
chmod +x Bottega-0.1.6-linux-x86_64.AppImage
./Bottega-0.1.6-linux-x86_64.AppImage
```

开始对话前，请先安装并登录至少一个受支持的本机 CLI。0.1.0 或 0.1.1 用户需要手动安装 0.1.6，因为旧版本仍有此前的更新器问题。所有旧版本升级前都需要完成上面的存储准备。

## 2026-09-19 — v0.1.5

**升级前请先备份：** 0.1.5 使用以 Bottega 文件夹为中心的新本地存储布局，不会导入 0.1.4 及更早版本的 Chat、Project、App、Base、附件与设置。请完全退出 Bottega，备份整个应用数据目录，并保留外部 Chat Homes 与项目目录。将旧应用数据目录移到备份位置后，再让 0.1.5 使用空白数据目录启动，并在初次设置中选择新的 Bottega 文件夹。旧版 Bottega 聊天、设置和已安装 App 记录不会自动导入，旧目录中的内容也不会被改动。操作方式见[备份与初始化说明](../getting-started/README.zh-CN.md#升级到-015)。

### 新增与改进

- **跨电脑同步，端到端加密。** 云同步是可选功能，在你主动开启前保持关闭，纯本地使用依然不需要账号。第一台电脑设置一个独立的同步密码，至少 8 个字符并同时包含英文字母和数字。加密内容的密钥在你自己的设备上用 Argon2id 派生，内容在上传前用 XChaCha20-Poly1305 封装，服务端只保存密文。没有恢复密钥，也没有密码重置，开启同步前 Bottega 会明确说明这一点。一个账号对应一个加密空间；第二台电脑用同一账号和同一密码加入。
- **在浏览器里打开工作区。** 在 [app.getbottega.app](https://app.getbottega.app) 登录后，可以阅读带工具过程和附件的 Chat，搜索聊天标题及最近 7 天的消息正文，使用 Base 的六种视图，读取和编辑已同步的 App 记录，恢复或删除归档项目，并管理设备与偏好。支持手机浏览器。App 自定义界面、应用内浏览器和本机工具仍在你的电脑上运行。
- **从另一台设备继续对话。** 远程操作在服务端开启后，可以从 Cloud Web 或手机浏览器驱动一台在线且已解锁的已登录桌面：选择电脑与 Agent、发送消息、实时查看回复、停止、批准或拒绝权限请求、回答追问、插话与追加消息。远程操作是服务端开关，而不是应用设置。关闭期间，浏览器里的 Chat 为只读，阅读转录和实时观看运行中的轮次仍然可用。
- **把工作保存在自己掌握的文件夹中。** 首次设置选择的 Bottega 文件夹保存 Chat 转录、原始附件、保存的产物、Chat Home 文件、Project 信息、Base 记录、App 源码与 Skills；账号设置、密钥和设备授权保留在各自电脑上。完整备份需要先退出 Bottega 再复制整个文件夹，之后可以在另一台电脑或全新 profile 中选择该副本。不支持 iCloud Drive、Dropbox 等网盘同步目录。本机 Chat 数据库无法打开时，启动可以在保留旧数据库的前提下从文件夹重建对话。
- **按自己的方式开始。** 引导流程先询问只在这台电脑上使用还是连接已有账号，再依次完成 Bottega 文件夹、Agent、Skills 与长期记忆。记忆设置在向导内原位完成，不再跳去设置页。
- **看到 Agent 产出的内容。** 可视化直接内联渲染并可在右栏打开；文件产物提供预览、保存、在文件夹中显示与快速查看；工作表可以导入 Base；Claude 的 artifact 在内置浏览器中打开。预览保持沙箱隔离，不获得网络或存储权限。
- **四家 Agent 的更多控制。** OpenCode Go 额度与 Codex、Claude Code、Kimi 一起进入设置页和 Agent 选择器，展示滚动、每周和每月三种窗口。标题生成改为指定一个明确的 Agent 与模型，四家均可选择。新增 **Settings › Lab**，其中的 **Keep Agent connections** 默认关闭：打开会话时预热 Agent 进程并在该会话的后续轮次中复用，代价是占用更多内存。
- **侧栏的日常操作。** 拖拽 Chat 排成你要的顺序，该顺序也在同步范围内。Project 可以直接重命名而不影响其内容，并按最近更新或手动排序。归档 Chat 或 Project 会撒花，可在 Settings › General 关闭，系统开启减少动态效果时自动暂停。

### 下载与安装

下方提供 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建**未签名、未公证**。macOS 仍是主要平台；Windows/Linux 原生 App 隔离及完整功能对齐仍在推进。

**macOS（Apple 芯片）：** 打开 DMG，把 Bottega 拖入「应用程序」。未签名下载需要在终端一次性移除该应用的隔离标记，然后再打开：

```bash
xattr -rd com.apple.quarantine /Applications/Bottega.app
```

**Windows（x64）：** 运行安装包。如果 SmartScreen 拦截未知发布者，点击**更多信息 → 仍要运行**。

**Linux（x64）：** 添加可执行权限后启动 AppImage：

```bash
chmod +x Bottega-0.1.5-linux-x86_64.AppImage
./Bottega-0.1.5-linux-x86_64.AppImage
```

开始对话前，请先安装并登录至少一个受支持的本机 CLI。0.1.0 或 0.1.1 用户需要手动安装 0.1.5，因为旧版本仍有此前的更新器问题。所有旧版本升级前都需要完成上面的存储准备。

## 2026-09-10 — v0.1.4

**升级前请先备份：** 0.1.4 使用新的本地存储格式，无法打开或自动迁移 0.1.3 及更早版本的数据。请完全退出 Bottega，备份整个应用数据目录，并保留外部 Chat Homes 与项目目录。将旧应用数据目录移到备份位置后，再让 0.1.4 使用空白目录初始化。旧版 Bottega 聊天、设置和已安装 App 记录不会自动导入。操作方式见[备份与初始化说明](../getting-started/README.zh-CN.md#升级到-014)。

### 新增与改进

- **直接在输入框画草图。** 通过 **+ → 草图** 打开画布，绘画、添加文字或八种图形，也可以局部擦除笔迹和图形。撤销、重做、颜色与粗细操作就在画布旁；保存后的草图在草稿和还原队列中仍可继续编辑，发送时作为白底 PNG 交给 Agent。
- **选择 Agent 前查看额度。** 设置页和 Agent 选择器展示 Codex、Claude Code、Kimi 可获得的配额窗口、剩余额度及重置时间；OpenCode 明确提示不提供统一额度信息。首次查询被失焦中断后，回到前台会继续读取；登录、安装和修复入口与 Agent 状态放在一起。
- **本地工作保存更可靠。** Chat、Base、Project、App 与附件存储补齐一致性和恢复规则，中断任务保留已记录状态。日常使用继续纯本地运行，无需云账号；本次版本不包含云同步服务。
- **第一方 App 界面更一致。** 开发看板、记账、健身和设计画布统一使用 React 与共享界面规范，保留各自数据与工作流程。随包 App 的最低宿主要求仍为 Bottega 0.1.3。
- **选择适合自己的后台入口。** 后台运行由一个开关管理。macOS 可选择 Bottega Logo 图标、黑白菜单栏图标或刘海任务面板；系统记住选择，没有刘海屏时自动使用图标。登录自启仍可单独设置，两项启动选项默认关闭。
- **完善日常操作。** 设置导航、字号、重命名、App 进度、失败恢复与分享弹窗提供更清晰的操作和一致布局。草图使用自适应方形画布与浮动控件，加载时在同一弹窗内原位过渡。

### 下载与安装

下方提供 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建**未签名、未公证**。macOS 仍是主要平台；Windows/Linux 原生 App 隔离及完整功能对齐仍在推进。

**macOS（Apple 芯片）：** 打开 DMG，把 Bottega 拖入「应用程序」。未签名下载需要在终端一次性移除该应用的隔离标记，然后再打开：

```bash
xattr -rd com.apple.quarantine /Applications/Bottega.app
```

**Windows（x64）：** 运行安装包。如果 SmartScreen 拦截未知发布者，点击**更多信息 → 仍要运行**。

**Linux（x64）：** 添加可执行权限后启动 AppImage：

```bash
chmod +x Bottega-0.1.4-linux-x86_64.AppImage
./Bottega-0.1.4-linux-x86_64.AppImage
```

开始对话前，请先安装并登录至少一个受支持的本机 CLI。0.1.0 或 0.1.1 用户需要手动安装 0.1.4，因为旧版本仍有此前的更新器问题。所有旧版本升级前都需要完成上面的存储准备。

## 2026-09-08 — v0.1.3

**升级前请先备份：** 0.1.3 使用新的本地存储格式，无法打开或自动迁移 0.1.2 及更早版本的聊天数据库。升级前请退出 Bottega，备份完整的应用数据目录，并保留原目录供旧版使用；启动 0.1.3 需要从空白数据目录重新初始化，原有 Bottega 聊天与设置不会自动导入。操作步骤见[备份与重新设置说明](../getting-started/README.zh-CN.md#升级到-013)。

### 新功能与改进

- **在同一聊天中切换 Agent。** 聊天空闲时，可以为下一轮选择 Codex、Claude Code、Kimi Code 或 OpenCode。对话保持在同一转录中，明确显示作者和切换标记；新的 Agent 获得有界上下文，并可按需读取相关聊天历史。
- **直接看到 Agent 是否可用。** 输入框展示安装、认证和运行时状态，并提供对应的安装、登录和重试入口。不可用的 Agent 不再静默消耗排队任务，恢复只作用于相关聊天或 Agent。
- **在 macOS 主窗口之外关注任务。** 登录自启、关闭窗口后继续运行、悬浮任务面板可以独立开启，默认均关闭。屏幕顶部面板展示运行中的任务和待处理请求，支持键盘导航，并可直接返回相关聊天。
- **安装前检查 App 兼容性。** 四个第一方 App 现均声明最低 Bottega 版本为 0.1.3。安装、重建、授权和激活前会检查版本要求；按提示升级并重启后，可以返回原 App 候选继续确认。被拒绝的更新会保留已有可用版本和授权。
- **App 改名不打断现有工作。** 修改显示名称时，保留当前运行版本、源码、数据和授权。
- **简化添加项目。** 只有发现可用的本机 CLI 历史时才询问是否导入，没有历史的项目可以直接添加。

### 下载

下方提供 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建仍未签名，请遵循[首次启动步骤](../getting-started/README.zh-CN.md)。macOS 仍是主要平台；Windows/Linux 的原生 App 隔离和完整功能对齐仍在推进。

0.1.0 或 0.1.1 用户需要手动下载并安装 0.1.3，因为旧版本仍包含此前的更新器问题。所有旧版本升级前都需要完成上述存储准备。

## 2026-09-05 — v0.1.2

**从 0.1.0 或 0.1.1 升级：** 请从 [Releases 页面](https://github.com/thinkingjimmy/Bottega/releases/tag/v0.1.2)手动下载并安装 0.1.2。旧版本仍包含本次修复的更新器问题，因此无法通过原有更新按钮获取这次修复。

- 修复未签名构建因缺少发布兼容性公钥而无法下载更新的问题。侧栏现在区分自动安装与手动下载，展示下载进度，并在更新或后台检查失败时保留前往 Releases 或 About 的入口。
- Fitness Log 重建在宿主 React 界面之上，使用共享组件和数据 API，同时保留 72 个动作、17 个肌肉区域、五语言、动作动图、训练计划，以及响应式深浅主题布局。
- App 数据加载完整且可恢复。Base 快照读取所有分页后才发布一致版本；Fitness 计划提交在重试或结果未知时保留原始行 ID，避免重复提交。
- 修复 Chat 内查找的重试与导航行为。分页失败等待显式重试，迟到响应无法覆盖新查询；切换 Chat 时也不再残留上一场对话的托管 worktree 分支。
- 修复旧 schema App 目录的启动流程：逐字节保留隔离副本，再建立空的当前目录；真正损坏的当前格式仍进入显式修复流程。同批完善暂存 turn 恢复与 Memory 取消处理。
- 发布 macOS arm64 DMG/ZIP、Windows x64 NSIS 和 Linux x64 AppImage 安装包。这批构建仍未签名，现有[首次启动步骤](../getting-started/README.zh-CN.md)继续适用。

## 2026-09-04 — v0.1.1

- 发布 v0.1.1 安装包：macOS arm64 DMG 与 ZIP、Windows x64 NSIS 安装包，以及 Linux x64 AppImage。这批产物仍未签名，各平台首次启动需要的一次性操作与 0.1.0 相同，仍见[快速开始](../getting-started/README.zh-CN.md)。
- 增加 Chat Fork。任意一条 assistant 回复都可以成为新 Chat 的起点，它之前的历史以只读方式继承；在 Git Project 上分叉还能获得一份产品托管的 worktree，同一场对话的两条分支不再互相覆盖同一个工作副本。
- 补齐 App 的使用侧。App Use 增加历史面板，App 也可以脱离主窗口、在独立窗口中运行。
- App GUI Surface 收敛为一套组件与一条消息通道，App 页面不再各自复制一份协议代码。
- 修复导入历史的两处真损坏。刷新导入历史不再清空 Chat 的 Project 归属；重新导入 session 会重写它的标题搜索文档，不再留下过期内容。
- 重排引导页。三步引导改为自适应窄窗口与容器感知的能力行，描述收敛为 chat home、Agent 与 extras 三段，在任何宽度下都保住可读的行宽。
- 让 Chat 存储的维护从「失败」改为「自愈」，也让真正的失败可被处理。与来源对话对不上的搜索投影会被重算，并经同一个写入口重写；自检确实失败时，侧栏给出带解决办法的类型化提示，以及一键打开预填 GitHub issue 的上报按钮。
- 让安装后的应用拥有自己的数据目录。从 Release 安装的副本现在把数据写入 `Bottega` 目录，不再与开发构建共用同一个目录，两者不会再互相重建本机状态。
- 让读不出的持久化账本自我恢复，而不是中止启动。内容不可信的账本会被改名隔离留证、以空态重建，应用照常启动。
- 随包交付的第一方 App preset 推进到各自已发布的提交。

## 2026-09-02 — v0.1.0

- 发布首批安装包。Bottega 现在可以从 GitHub Releases 获取 macOS arm64 DMG 与 ZIP、Windows x64 NSIS 安装包，以及 Linux x64 AppImage，全部由本次 tag 对应的提交构建。这批产物未签名，首次启动时各平台需要的一次性操作见[快速开始](../getting-started/README.zh-CN.md)。
- Chat 存储重建在 SQLite 之上，并以它为唯一真相源。对话、turn、附件与 fact 统一落在一个持久化本地数据库里，不再散落为逐 Chat 的文件；Chat 因此能在崩溃后存活、无需重新扫描即可恢复，也不再越长越慢。
- 让长对话的打开成本恒定。时间线、Chat 大纲与 Chat 内查找全部分页：打开一个有几万 turn 的 Chat 与打开一个短 Chat 代价相同，向上回溯也不会重新加载整份转录。
- 增加基于 gram 的全文搜索。搜索对中文、日文、韩文的匹配与空格分词语言一样可靠，且与转录读取同一份存储。
- 导入历史并入同一条时间线。从本机 Codex、Claude Code、Kimi Code 与 OpenCode CLI 接续的 session，现在与 Bottega 内创建的 Chat 使用同一份转录、大纲、搜索与导航，不再是一个独立的只读视图。
- 收窄 fact 写入。一个 turn 只更新它真正拥有的 fact，并发 turn、Memory 交付与 Base 写入不再互相覆盖状态。
- 关闭合并评审发现的问题。App Use 只在收到 completed 回执后才导航，被拒绝或恢复中的 App 不会再移动窗口；撤销 App 的 Base 访问权限成为一次原子操作，访问权与生命周期不再出现半状态；App 与 Project 的固定、Project 外观与设置导航重新组织，侧边栏始终反映真正打开的内容。

## 2026-08-29 — Project 级工具、Extension 与 Design Canvas 源码预览

- 在干净公开历史上以正常子提交发布当前生产源码；测试、开发自动化与内部证据继续只留在 Bottega-Dev。
- 内置工具与手动 MCP server 支持精确 Project 覆盖。每个 turn 在副作用开始前冻结最终工具计划、scope revision、runtime 支持事实与 sealed MCP 配置。
- Extension 统一采用 `global | exact Project` 所有权，贯穿管理、Skill、App requirement、session、retained data 与删除恢复。精确空的旧 Extension Registry、lifecycle ledger 与 projection ledger 可向前迁移；任何携带 live 或歧义授权事实的旧状态继续 fail closed。
- 增加随包交付的 Bottega Design Canvas，支持自包含 HTML 画板、方向与历史比较、编号视觉锚点、沙箱预览与 Agent 侧 render check。
- 四个第一方 App gitlink 全部推进到远端可达提交。本次是源码预览，不是仍受发布门禁约束的正式 `v0.1.0` 安装包 Release。

## 2026-08-25 — 公开源码发布

- Bottega 以 MIT License 发布，并使用只包含公开内容的全新 Git 历史。
- 建立硬性仓库边界：生产桌面源码与里程碑文档公开；测试、测试数据、Web 应用、内部评估、TODO、开发笔记、周度工程日志与仓库自动化保留在开发仓库。
- 公开文档统一进入 docs/，并按快速开始、功能、更新日志组织二级目录；根 README 只承担 GitHub 仓库入口职责。
- 产品、package、窗口、构建产物、ACP client 与导出文档统一使用 **Bottega** 身份。

## 2026-08-18 至 2026-08-23 — 可持久化协作

- 工作空间引用从 Chat 扩展到文件与 Section。
- 支持 Section 之间持久化交接图片，并把 Subagent 结果提升为可复用、默认 idle 的 Section。
- 统一管理 Codex、Claude Code、Kimi Code 与 OpenCode 的本地 Skills。
- 增加可搜索的只读历史联邦，并支持接续本机 Agent session。

## 2026-08-08 至 2026-08-23 — 显式同意的 Memory

- 增加产品托管的本机 OpenViking 与 EverOS provider。
- 引入 Chat、Project group 与个人三级共享范围，配套显式同意和可观测交付状态。
- 增加重建、来源、模型下载进度与可信版本切换。

## 2026-08-04 至 2026-08-21 — App、工具与浏览器

- 增加由进程内 CDP 控制的多标签页内置浏览器。
- 内置工具平台扩展到 Section、搜索、Base、文件、App 与浏览器动作。
- 统一 static、server 与 Base-backed App，并提供与 generation 绑定的权限和受约束 GUI SDK。

## 2026-07-28 至 2026-08-23 — Base

- 为 Chat 与 Project 引入结构化数据，支持 Table、List、Kanban、Map、Chart 与 Gallery 六种视图。
- 增加公式、relation、附件、行历史、导入导出，以及 capability 有界的 App 写入。

## 2026-07-16 至 2026-08-09 — 桌面端与 Multi-agent 基础

- 从 Web 原型迁移为 Electron 桌面工作台。
- 通过本机 CLI 与 ACP 连接 Codex、Claude Code、Kimi Code 和 OpenCode，同时保留 CLI 凭据主权。
- 增加流式 turn、审批、Plan、消息 Steer、Subagent、Project 工作空间、归档语义与 OS 级文件边界。
